import React from "react"
//import "../components/animation.scss"

/*
 * This component is built using `gatsby-image` to automatically serve optimized
 * images with lazy loading and reduced file sizes. The image is loaded using a
 * `useStaticQuery`, which allows us to load the image from directly within this
 * component, rather than having to pass the image data down from pages.
 *
 * For more information, see the docs:
 * - `gatsby-image`: https://gatsby.dev/gatsby-image
 * - `useStaticQuery`: https://www.gatsbyjs.org/docs/use-static-query/
 */

const HeroAnimation = () => (
  
    <div className="medium-10 large-5 cell large-offset-1">
        <div className="osoc-float-element-o"></div>
        <div className="osoc-float-element-s"></div>
        <div className="osoc-float-element-o-small"></div>
        <div className="osoc-float-element-c"></div>
    </div>

)

export default HeroAnimation
